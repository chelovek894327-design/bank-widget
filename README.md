# Bank Widget

## 📌 Описание проекта

Проект представляет собой backend-часть банковского виджета, который отображает последние банковские операции клиента.

Система позволяет:
- маскировать номера банковских карт и счетов;
- фильтровать операции по статусу;
- сортировать операции по дате;
- форматировать дату для отображения.

---

## ⚙️ Установка проекта

### 1. Клонировать репозиторий

```bash
git clone https://github.com/chelovek894327-design/bank-widget.git
```

### 2. Перейти в папку проекта

```bash
cd bank-widget
```

### 3. Установить зависимости

```bash
poetry install
```

---

## 🚀 Запуск тестов

```bash
poetry run pytest
```

### Проверка покрытия тестами

```bash
poetry run pytest --cov=src --cov-report=html
```

После выполнения команды будет создана папка `htmlcov` с HTML-отчетом.

---

## 🧩 Основные функции

### 🔹 Маскировка номера карты

```python
get_mask_card_number("1234567812345678")
```

Результат:

```text
1234 56** **** 5678
```

---

### 🔹 Маскировка номера счета

```python
get_mask_account("12345678901234567890")
```

Результат:

```text
**7890
```

---

### 🔹 Фильтрация операций по статусу

```python
filter_by_state(operations, "EXECUTED")
```

Результат:
- только операции со статусом `EXECUTED`.

---

### 🔹 Сортировка по дате

```python
sort_by_date(operations)
```

Результат:
- список операций, отсортированный от новых к старым.

---

### 🔹 Форматирование даты

```python
get_date("2024-03-11T02:26:18.671407")
```

Результат:

```text
11.03.2024
```

---

### 🔹 Маскировка строки карты/счёта

```python
mask_account_card("Visa Platinum 7000792289606361")
```

Результат:

```text
Visa Platinum 7000 79** **** 6361
```

---

## 📁 Структура проекта

```text
bank-widget/
│
├── src/
│   ├── masks.py
│   ├── processing.py
│   └── widget.py
│
├── tests/
│   ├── test_masks.py
│   ├── test_processing.py
│   └── test_widget.py
│
├── README.md
└── pyproject.toml
```

---

## 🧠 Используемые технологии

- Python 3.10+
- Poetry
- Pytest
- Flake8
- MyPy